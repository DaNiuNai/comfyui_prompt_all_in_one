# Vendored data

`default.yaml`, `zh_CN.yaml`, and `translate_apis.json` are derived from
Physton/sd-webui-prompt-all-in-one and remain subject to that project's
MIT license. They are included to preserve the original prompt library
and online translation provider metadata.
